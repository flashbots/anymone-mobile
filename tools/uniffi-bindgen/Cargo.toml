[package]
name = "anymone-uniffi-bindgen"
description = "uniffi-bindgen CLI pinned to the uniffi version the bindings are built with"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
publish = false

[dependencies]
uniffi = { workspace = true, features = ["cli"] }
